<%- include('../views/partials/header.ejs') %>

    <div class="container">
        <div class="row">
            <div class="col">
                <% if(home.length> 0) { %>
                    <p>
                        <%= home.address %>
                    </p>
                    <p>
                        <%= home.price %>
                    </p>
                    <% } %>
            </div>
        </div>
    </div>

    <%- include('../views/partials/footer.ejs') %>